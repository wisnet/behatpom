{{#each suites}}
    {{#each features}}
       {{#each scenarios}}    
         {{#each steps}}
           {{#if img}}
{{img}} screenshots/{{device}}{{img}}.png
  	   {{/if}}
         {{/each}}  
       {{/each}} 
    {{/each}}
{{/each}}
